/**
 * Stub for @deephaven/jsapi-types
 * Type-only package in the real codebase; the bundle only needs `dh` as a
 * type import.  At runtime this is never used (imported types are erased).
 */
export const dh = {};
export default {};
